# gitsweeper-billbird-mcp

Expose Gitsweeper's PR analytics and Billbird's hours/plan/cycle-time data as MCP tools so managers can query both via an AI client (Claude Desktop, etc.). Read-only v1, Billbird reached over its REST API with a bearer token.
